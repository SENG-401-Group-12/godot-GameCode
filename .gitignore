# Local secrets for Godot / Supabase (see farming-game/.env.example)
farming-game/.env

# Cursor IDE — local only (MCP config under .cursor/; optional local template not tracked)
.cursor/
godot-mcp.cursor.example.json
.DS_Store
